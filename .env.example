# Copy to .env and fill in values as needed
# IMPORTANT: For production, do not expose provider keys in client code. Proxy through a server.

# Used by aiService.ts and imageEditingService.ts (demo only)
VITE_GEMINI_API_KEY=

# Example (if you implement server-side /api/social-writeup)
# OPENAI_API_KEY=
